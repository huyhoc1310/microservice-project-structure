[default]
SECRET = 'this is default secret'

[development]
SECRET = 'this is development secret, rollback to default if not specified'

[production]
SECRET = 'this is production secretm, rollback to default if not specified'
